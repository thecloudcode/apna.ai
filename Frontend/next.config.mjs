/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['avatars.githubusercontent.com', 'media.licdn.com/', 'assets.aceternity.com', 'media.designrush.com', 'upload.wikimedia.org'],
    },
  };
  
  export default nextConfig;
  